schema_version = 1
policy = "generated-allowlist"
owner = "EffortlessMetrics"
status = "advisory"

# Files marked `linguist-generated=true` via .gitattributes. Reviewers
# should see them collapsed by default; CI should not flag drift in their
# contents as a manual change.
#
# Skeleton — entries shape:
#
# [[allow]]
# id = "generated-0001"
# path = "policy/no-panic-baseline.toml"
# generator = "cargo xtask no-panic baseline --reset"
# regenerate_command = "cargo xtask no-panic baseline --reset"
# owner = "policy"
# reason = "Generated by the no-panic classifier; reset only in dedicated baseline PRs."
# created = "2026-05-09"
# expires = "permanent"
#
# Today (post-#141), no files are marked linguist-generated. The first
# entry will be added in PR #151 when the no-panic baseline is generated.
