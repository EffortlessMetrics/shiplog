# Implementation Plans

Plans are PR-sized execution sequences. They own:

- dependency ordering;
- PR scope and non-goals;
- proof commands;
- rollback instructions;
- claim boundaries.

Plans do not redefine product motivation (proposal) or behavior contracts (spec).
