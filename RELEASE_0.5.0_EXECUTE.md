# shiplog 0.5.0 — Final Release Execution

**Status:** Ready to publish to crates.io  
**Merge Commit:** `97347c89df91c1c11bb2169be90428a8c68c3a38`  
**Tag:** `v0.5.0` (created locally)  
**Build:** `target/release/shiplog 0.5.0` ✅

---

## Pre-Publish Verification Complete

```bash
✅ package-proof.sh PASS         # tests, audit, deny, fmt, clippy
✅ cargo publish -p shiplog-ids --dry-run PASS
✅ cargo build --release -p shiplog PASS
✅ Binary smoke tests PASS       # --version, --help, intake --help
✅ v0.5.0 tag created locally
✅ PR #157 merged on main (SHA 97347c89df91c1c11bb2169be90428a8c68c3a38)
✅ All 24 crates at version 0.5.0
✅ CHANGELOG.md finalized
✅ Release docs complete
```

---

## Execute the Release

### 1. Publish to crates.io (automated)

```bash
# Set your crates.io API token
export CARGO_REGISTRY_TOKEN="<your-token>"

# Run the automated publish script
./scripts/publish-v0.5.0.sh
```

The script publishes in dependency order:
- Foundation: `shiplog-ids`, `shiplog-schema`, `shiplog-ports`
- Trust surface: `shiplog-coverage`, `shiplog-cache`, `shiplog-redact`, `shiplog-bundle`, `shiplog-workstreams`, `shiplog-render-md`, `shiplog-render-json`
- Adapters: `shiplog-cluster-llm`, `shiplog-ingest-*` (6 crates), `shiplog-team`, `shiplog-merge`
- Orchestration: `shiplog-engine`
- CLI: `shiplog`

**Total: 23 crates + CLI = all public packages**

### 2. Verify all crates uploaded

```bash
# Check crates.io release page
https://crates.io/crates/shiplog/versions
# Should show v0.5.0 across all crates
```

### 3. Tag and release on GitHub (post-publish)

```bash
git tag v0.5.0
git push origin v0.5.0
# Triggers release.yml: builds multi-platform binaries, creates GitHub release
```

### 4. Post-tag smoke tests

```bash
scripts/release-install-smoke.sh v0.5.0           # Linux/macOS/Windows validation
scripts/release-install-smoke.ps1 -Version v0.5.0  # PowerShell parity on Windows
```

### 5. Announce

GitHub release notes auto-generated by `release.yml`. Update doc sites, dependent projects, and post announcement.

---

## Rollback (if needed)

**Pre-publish:** Delete tag: `git tag -d v0.5.0`; revert PR #157 if necessary.  
**Post-partial publish:** Yank uploaded crates: `cargo yank --version 0.5.0 -p <crate>`.  
**Post-full publish:** Cut 0.5.1 with the fix, or yank all and start 0.6.0.

See [`docs/release/0.5.0-readiness.md#rollback-path`](docs/release/0.5.0-readiness.md#rollback-path) for details.

---

## Summary

**Everything is ready.** The codebase is at v0.5.0, all gates cleared, binary built and tested. Just provide your crates.io token and run the publish script.
