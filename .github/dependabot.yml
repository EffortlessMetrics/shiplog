# Product-changing dependency automation is authoritative in
# EffortlessMetrics/shiplog-swarm. This release/source repository verifies the
# promoted tree and owns release operations; it must not open code PRs.
version: 2
updates: []
