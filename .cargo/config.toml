# Cargo configuration for shiplog project

[profile.dev]
# Optimize dependencies in dev mode for faster test runs
opt-level = 1

[profile.test]
# Optimize test builds for speed
opt-level = 1

[profile.bench]
# Benchmark profile
inherits = "release"
debug = true
