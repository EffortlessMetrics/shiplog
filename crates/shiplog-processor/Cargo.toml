[package]
name = "shiplog-processor"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Processor utilities for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-processor"
repository.workspace = true
homepage.workspace = true
keywords = ["shiplog", "processor", "pattern", "pipeline", "processing"]
categories = ["development-tools", "rust-patterns"]

[dependencies]
anyhow.workspace = true

[dev-dependencies]
proptest.workspace = true
