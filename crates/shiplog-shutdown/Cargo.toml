[package]
name = "shiplog-shutdown"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Graceful shutdown utilities for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-shutdown"
repository.workspace = true
homepage.workspace = true
keywords = ["shiplog", "shutdown", "concurrency", "async", "synchronization"]
categories = ["concurrency", "asynchronous"]

[dependencies]
tokio = { workspace = true, features = ["sync", "time", "rt-multi-thread", "macros"] }
anyhow.workspace = true
