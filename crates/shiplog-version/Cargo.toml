[package]
name = "shiplog-version"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Version parsing and comparison utilities for shiplog."
readme = "README.md"

[dependencies]
