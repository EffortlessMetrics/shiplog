# shiplog-notify

Notification contracts used by shiplog and related microservices.

- Stable `Notification` envelope and `Notifier` trait
- Default `NotificationService` with composable notifier registration
- Console notifier implementation for local development and test environments

## Compatibility

Designed for deterministic behavior and easy extension to richer backends.
