[package]
name = "shiplog-union"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Union operations for combining streams in shiplog."
readme = "README.md"

[dependencies]
anyhow.workspace = true
