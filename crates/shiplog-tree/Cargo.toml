[package]
name = "shiplog-tree"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Generic tree implementations (Binary, AVL, Red-Black) for shiplog."
readme = "README.md"

[dependencies]
anyhow.workspace = true
