[package]
name = "shiplog-redact"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Deterministic profile-based redaction engine for shiplog events and workstreams."
readme = "README.md"
documentation = "https://docs.rs/shiplog-redact"
repository.workspace = true
homepage.workspace = true
keywords = ["redaction", "hmac", "privacy", "anonymization", "deterministic"]
categories = ["development-tools", "text-processing"]

[dependencies]
anyhow.workspace = true
serde_json.workspace = true
shiplog-alias = { path = "../shiplog-alias", version = "0.2.1" }
shiplog-redaction-projector = { path = "../shiplog-redaction-projector", version = "0.2.1" }
shiplog-schema = { path = "../shiplog-schema", version = "0.2.1" }
shiplog-ports = { path = "../shiplog-ports", version = "0.2.1" }

[dev-dependencies]
proptest.workspace = true
chrono.workspace = true
tempfile.workspace = true
shiplog-ids = { path = "../shiplog-ids", version = "0.2.1" }
shiplog-testkit = { path = "../shiplog-testkit", version = "0.2.1" }
shiplog-redaction-policy = { path = "../shiplog-redaction-policy", version = "0.2.1" }


