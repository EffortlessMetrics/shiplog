[package]
name = "shiplog-timeout"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Timeout utilities for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-timeout"
repository.workspace = true
homepage.workspace = true
keywords = ["shiplog", "timeout", "concurrency", "async", "synchronization"]
categories = ["concurrency", "asynchronous"]

[dependencies]
tokio = { version = "1.49.0", features = ["rt", "macros", "time", "sync"] }
anyhow.workspace = true

[dev-dependencies]
proptest.workspace = true
