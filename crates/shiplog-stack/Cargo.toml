[package]
name = "shiplog-stack"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Stack data structure implementations (Array, Linked List) for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-stack"
repository.workspace = true
homepage.workspace = true
keywords = ["stack", "data-structure", "lifo", "array", "linked-list"]
categories = ["data-structures", "development-tools"]

[dependencies]
anyhow.workspace = true

[dev-dependencies]
proptest.workspace = true
