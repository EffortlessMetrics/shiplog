[package]
name = "shiplog-redaction-repo"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Canonical public-profile repository redaction contract for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-redaction-repo"
repository.workspace = true
homepage.workspace = true
keywords = ["redaction", "repo", "privacy", "alias", "policy"]
categories = ["development-tools", "text-processing"]

[dependencies]
shiplog-schema = { path = "../shiplog-schema", version = "0.2.1" }

[dev-dependencies]
insta.workspace = true
proptest.workspace = true
serde_json.workspace = true
shiplog-alias = { path = "../shiplog-alias", version = "0.2.1" }
shiplog-testkit = { path = "../shiplog-testkit", version = "0.2.1" }

[lints]
workspace = true
