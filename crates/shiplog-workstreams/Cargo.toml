[package]
name = "shiplog-workstreams"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Repo-based clustering and curated/suggested workstream file management for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-workstreams"
repository.workspace = true
homepage.workspace = true
keywords = ["workstreams", "clustering", "yaml", "curation", "pipeline"]
categories = ["development-tools", "filesystem"]

[dependencies]
anyhow.workspace = true
chrono.workspace = true
serde.workspace = true
serde_yaml.workspace = true
shiplog-ids = { path = "../shiplog-ids", version = "0.2.1" }
shiplog-schema = { path = "../shiplog-schema", version = "0.2.1" }
shiplog-ports = { path = "../shiplog-ports", version = "0.2.1" }

[dev-dependencies]
tempfile = "3"


