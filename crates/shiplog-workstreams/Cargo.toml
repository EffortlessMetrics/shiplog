[package]
name = "shiplog-workstreams"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Repository-based event clustering and workstream management for shiplog"
repository.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
serde.workspace = true
serde_yaml.workspace = true
shiplog-ids = { path = "../shiplog-ids", version = "0.2.0" }
shiplog-schema = { path = "../shiplog-schema", version = "0.2.0" }
shiplog-ports = { path = "../shiplog-ports", version = "0.2.0" }

[dev-dependencies]
tempfile = "3"

