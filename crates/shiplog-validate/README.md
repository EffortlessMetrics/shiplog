# shiplog-validate

Validation primitives for shiplog events, payloads, and packet envelopes.

- Pure validation functions with stable error models
- Deterministic packet sanity checks that are easy to consume from adapters and CLI commands

## Compatibility

The crate is designed to be dependency-light and straightforward for use across
microcrate boundaries.
