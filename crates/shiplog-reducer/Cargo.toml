[package]
name = "shiplog-reducer"
version = "0.2.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Reducer utilities (like Redux) for shiplog."
readme = "README.md"
documentation = "https://docs.rs/shiplog-reducer"
repository.workspace = true
homepage.workspace = true
keywords = ["shiplog", "reducer", "pattern", "pipeline", "processing"]
categories = ["development-tools", "rust-patterns"]

[dependencies]
anyhow.workspace = true

[dev-dependencies]
proptest.workspace = true
