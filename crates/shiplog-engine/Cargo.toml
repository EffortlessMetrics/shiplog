[package]
name = "shiplog-engine"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
description = "Orchestration engine for the shiplog pipeline (ingest, cluster, redact, render)"
readme = "README.md"
documentation = "https://docs.rs/shiplog-engine"
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true

[dependencies]
anyhow.workspace = true
chrono.workspace = true
serde_yaml.workspace = true
shiplog-ids = { path = "../shiplog-ids", version = "0.2.0" }
shiplog-schema = { path = "../shiplog-schema", version = "0.2.0" }
shiplog-ports = { path = "../shiplog-ports", version = "0.2.0" }
shiplog-workstreams = { path = "../shiplog-workstreams", version = "0.2.0" }
shiplog-redact = { path = "../shiplog-redact", version = "0.2.0" }
shiplog-render-md = { path = "../shiplog-render-md", version = "0.2.0" }
shiplog-render-json = { path = "../shiplog-render-json", version = "0.2.0" }
shiplog-bundle = { path = "../shiplog-bundle", version = "0.2.0" }

[dev-dependencies]
tempfile.workspace = true

