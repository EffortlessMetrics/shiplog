# shiplog .gitattributes
#
# Mark generated files so reviewers see them collapsed by default and
# linguist treats them appropriately. See docs/FILE_POLICY.md and
# policy/generated-allowlist.toml.

# no-panic baseline — generated by `cargo xtask no-panic baseline --reset`
# in dedicated PRs only (do not hand-edit). See docs/NO_PANIC_POLICY.md.
policy/no-panic-baseline.toml text eol=lf linguist-generated=true
